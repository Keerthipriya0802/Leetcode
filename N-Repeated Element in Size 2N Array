class Solution(object):
    def repeatedNTimes(self, nums):
        l=len(nums)
        for i in range(0,l):
            count=0
            for j in range(i+1,l):
                if(nums[i]==nums[j]):
                    n=nums[i]
                    return n
