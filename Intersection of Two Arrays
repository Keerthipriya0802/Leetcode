class Solution(object):
    def intersection(self, nums1, nums2):
        l=len(nums1)
        inter=[]
        for i in range(0,l):
            if(nums1[i] in nums2):
                inter.append(nums1[i])
        s=set(inter)
        inter=list(s)
        return inter
