class Solution(object):
    def averageValue(self, nums):
        lst=[]
        for i in nums:
            if(i%2==0 and i%3==0):
                lst.append(i)
        if not lst:
            return 0
        l=len(lst)
        sums=sum(lst)
        average=sums/l
        return average
